---
type: architecture_domain_model
project: "Alex Bobadilla black seller"
status: draft
bounded_contexts:
  # Each entry requires 'name'. Optional: description, modules.
  # Shorthand (just the name) is also accepted.
  # Examples:
  #   - name: conversation
  #     description: Tool-calling LLM loop
  #     modules: [run_agent, context_engine]
  #   - payments
  []
shared_kernel:
  # modules: list of module names shared across bounded contexts.
  # Examples:
  #   modules:
  #     - constants
  #     - logging
  modules: []
---

# Domain Model: Alex Bobadilla black seller

> Bounded contexts and shared kernel
> Fill with /rai-project-create or /rai-project-onboard

## Bounded Contexts

<!-- Group modules into bounded contexts based on domain boundaries -->

## Shared Kernel

<!-- Modules shared across bounded contexts -->
